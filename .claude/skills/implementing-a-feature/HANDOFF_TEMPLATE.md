# Handoff: <slug>

<!-- This file is rewritten in full on every iteration of work on this branch - it always
     reflects the branch's current, final state, as if it had been written this way from the
     start. Do not append change-by-change narration, and do not describe earlier iterations or
     abandoned approaches that no longer apply to the branch as it stands. See
     implementing-a-feature (step 6) for when this is written, and creating-pull-requests for
     how it feeds the PR description.

     Required sections: Objective/Problem, Scope, How to Validate - every handoff needs these.
     Optional sections: Design Decisions, Trade-offs & Known Follow-ups, Rollout/Migration Risks -
     omit a section entirely (heading and all) when nothing in it applies to this branch, rather
     than keeping it with "N/A" or leaving it empty. -->

## Objective / Problem

<!-- What this branch solves, and why - the problem/motivation from the driving issue/spec,
     restated in your own words if it clarifies. -->

## Scope

<!-- What changed, by module or file - enough for a reviewer to know where to look without
     reading the whole diff first. -->

## Design Decisions

<!-- Every open question resolved during implementation: the question, the resolution, and the
     reasoning. Include alternatives that were considered and discarded, and why - this is the
     one place that context survives once the PR ships (a PR description only carries the final
     state, not the road not taken). If a resolution is itself an accepted decision the code must
     keep following, it should also have its own ADR (per AGENTS.md "Development Workflow") -
     link it here rather than duplicating its content. -->

## Trade-offs & Known Follow-ups

<!-- Anything deliberately deferred, simplified, or left as a known limitation - and why that was
     an acceptable trade-off for this change. This is a record of what was consciously not done
     here, not a TODO list to work through next. If something here warrants tracked follow-up
     work, open a GitHub issue for it (per AGENTS.md "Development Workflow") rather than leaving
     it only as a line in this file. -->

## How to Validate

<!-- Automated: which suites/commands actually cover this (see AGENTS.md "Test Execution Scope"
     for how much to run and when). Manual: concrete steps a reviewer can follow, including any
     device/browser-specific checks (e.g. push notification permission prompts) with no automated
     coverage. -->

## Rollout / Migration Risks

<!-- Anything that affects deploy safety: a new migration, a config/env var change, a behavior
     change for existing data, a step that must happen in a particular order relative to release. -->
